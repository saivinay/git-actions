name: Shell commands

on: [push]

jobs:
  run-shell-commands:
      runs-on: ubuntu-18.04
      steps:
        - name: echo a string
          run: echo "hello world"
        - name: multiline script
          run: | 
              node -v
              npm -v
              ifconfig